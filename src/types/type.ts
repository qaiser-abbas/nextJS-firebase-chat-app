export interface HeroSectionType {
  title: string;
  subtitle: string;
  imageUrl: string;
  buttonText: string;
  buttonLink: string;
}

export interface FeatureItem {
  icon: string; // image path like '/images/cloud.png'
  title: string;
  description: string;
}
